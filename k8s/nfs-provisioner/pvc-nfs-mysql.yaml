apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-nfs-mysql-1
  annotations:
   volume.beta.kubernetes.io/storage-class: "managed-nfs-storage"  # <  the dynamic nfs storage call we have created earlier
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi