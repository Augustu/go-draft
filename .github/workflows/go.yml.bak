name: go-draft
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Set up Go
        uses: actions/setup-go@v2
        with:
          go-version: 1.16

      # nothing to build
      # - name: Build
      #   run: go build -v ./..

      - name: Test
      - run: ls /home/runner/work/go-draft
      - run: go test -v ./..

